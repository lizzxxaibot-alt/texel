# Texel — the action ledger

Same mechanism as `pixelkiln\launch\watch\ACTIONS.md`, and it exists for the same
reason (2026-09-09, user): **`texel-watch` judges and never fixes, which is
correct, but a judge with no bailiff produces nothing.** On the Pixelkiln side
three consecutive watch runs named the same findings and none of them moved for
eight days. This file is what stops that happening here while the venture is new
enough to fix cheaply.

## The rules

- **`texel-watch` is the only routine that OPENS a row, and the only one that
  CLOSES one.** It closes against evidence a doer wrote, never a promise.
- **Every doer — `texel-release`, `texel-support`, `texel-marketing`,
  `texel-funnel` — clears its open rows BEFORE its own agenda.** Section 0 of
  each SKILL.md.
- **A doer never closes a row.** It does the work, appends evidence, sets state
  to `DONE?`.
- **Owner `HUMAN`** = credentials, money, a payout or tax form, an account
  signup. Never assigned to a routine, never retried by one.

## The escalation ladder

| Age | What happens |
|---|---|
| 0–2 days | Normal. The owner clears it next run. |
| **3+ days** | **The owner routine may do NOTHING ELSE until the row is cleared, or it writes a dated refusal with a reason.** |
| **7+ days** | `texel-watch` toasts the user. Only rows this old, and `HUMAN` rows. |
| **14+ days** | Watch stops re-reporting and must recommend **killing the finding or killing the routine that will not do it.** |

### The 7-day and 14-day rungs are SUSPENDED for `HUMAN` rows, 2026-09-23, and the suspension has an end condition

**`texel-watch` run 12 actually called the escalation tool rather than assuming it
works, and it does not.** `PushNotification` returns *"Mobile push not sent
(Remote Control inactive)"*, and the Windows toast tool the routine's task file
names does not exist in session. So the **7+ days → "`texel-watch` toasts the
user"** rung has never fired for any row in this file, and the **14+ days →
recommend killing the finding or the routine** rung would be judging an owner who
was **never actually asked**. `pixelkiln-watch` reached the identical finding on
the identical evidence and suspended the same rung for its own six `HUMAN` rows
(`STATE.md`, run 19).

**Suspended for `T-010`, `T-011`, `T-015` and `T-016`** — the rows whose owner is
`HUMAN`. (This note originally named **T-002** as a fifth; the user re-filed that
row store-wide the same day and it is now closed here and open as
`pixelkiln/launch/watch/ACTIONS.md` **A-041**, where the same suspension applies
for the same reason.) Rows owned by a doer routine keep the full ladder,
because a doer routine *is* reachable.

**The end condition is not "when the channel feels fixed".** It is
`pixelkiln/launch/watch/ACTIONS.md` **A-032** — the fleet-wide row that owns the
escalation path itself — **closing**. On the day A-032 closes, the 7-day and
14-day rungs resume for every `HUMAN` row in this file at their true ages, with no
credit for the suspension. That boundary is stated because `venture-critic`
returned SERIOUS on a draft that suspended the rung with no re-trigger: an
unbounded exemption is how a rule stops biting, and this file already carries two
rows (**T-011**, **T-015**) opened for exactly that shape of defect.

**No row is opened here for the broken channel.** A-032 owns it fleet-wide and a
second row against one question splits the fix.

## Open

| id | opened | owner | action | done when |
|---|---|---|---|---|
| T-008 | 2026-09-11 | `texel-release` | **Blender middle-elides the density readout in the sidebar, so the one number the product is positioned on cannot be read in the panel that prints it.** It renders as `10.5 px/unit av....1, 8.4x spread)` — the N-panel region is 280 px and `Region.width` is read-only, so the average, the range and the spread cannot all be shown at any window size. Reported by `texel-marketing` while building the density card (T-001) and in `LISTING.md` lines 398-403, explicitly handed off as *"not this routine's lane"* — so it has been named twice by a doer and owned by nobody. **This routine has not reproduced it**; it is opened because a product defect on the marquee feature with no owner is exactly what this ledger exists to stop. The listing works around it by setting the figures as type on the card; the buyer who installs the add-on gets the elided string. | `texel-release` either ships a readout whose average, range and spread are all legible in the sidebar at its default width (split label/value rows, a multi-line `layout.label` stack, or the figures in a popover), **or** `ROADMAP.md` carries a dated line saying it will not be fixed and why — a reproduction attempt that fails also closes it, written up with the Blender version. **2026-09-17, `texel-watch` run 8 — NOT CLOSED, and the reason is one step, not more engineering.** The fix is real and is in `dist/texel-0.2.1.zip`: `core/report.py` returns three lines, one measurement each (`core/report.py` builds it as an average line, a `range lo - hi` line and a spread line, joined by newlines instead of packed into one 41-character string), reproduced on 4.5.9 before and after. **That zip was never uploaded**, and this row's done-when says *ships*, not *builds* — a buyer who pays $9.95 today still gets `10.5 px/unit av....1, 8.4x spread)`. Verified by extracting both archives: `core/report.py` is present in 0.2.1 and absent from the live 0.2.0. **Blocked on T-012, not on further engineering** — do not re-diagnose this row from scratch. **DONE? 2026-09-24, `texel-release`.** The blocker is gone: `texel-0.2.1.zip` is live and `core/report.py` is in it. A buyer who installs today gets the three-line readout, one measurement per row, drawn through `tex_ui._draw_status` -> `report.status_lines` at a 24-character budget set below the 34 that survived the 4.5.9 reproduction, because a budget set at the last passing case is not a budget. `core/report.py` is at **100% coverage for the first time today** - it shipped on 09-16 at 0%. Evidence for the upload is the same four readings as T-012 and is not duplicated here. **What this row does NOT prove:** the verification is of the zip on the page, not a fresh screenshot of the sidebar on this build. Nobody re-shot the panel this run. <br><br>**2026-09-25, `texel-watch` run 13 — NOT CLOSED, and the gap is one screenshot.** Verified this run, independently of the doer: the logged-out page names `texel-0.2.1.zip` / `200 kB`; `dist/texel-0.2.1.zip` and `dist/shipped/texel-0.2.1-SHIPPED-2026-09-24.zip` are byte-identical (sha256 `e3cd2748…cbafaa2c`, 205,427 B), and that zip carries `texel/core/report.py` with `LIMIT = 24` and a three-line `density_status`. **The done-when is visual — *legible in the sidebar at its default width* — and nobody has looked at the panel on the shipped build**: the doer's own DONE? says the panel was not re-shot, and this run did not render it either. A character budget under a measured 34 is strong inference, not a look; `venture-critic` returned FATAL on a draft that closed the row on it. **Closes when** a screenshot of the N-panel at default width, Blender 4.5.9, shipped 0.2.1 zip installed, shows all three lines unelided, and its path is written here. **14-day rung: recommend neither kill.** The engineering is done and shipped and the finding is one render from closed; killing the finding would discard a nearly paid-for check, and killing the routine would punish an owner that cleared this row's substance, T-012 and T-013 in one run. Owner stays `texel-release` (next slot Wed 2026-09-30); `texel-marketing`, which first reported the elision and runs daily, may supply the shot as evidence. <br><br>**2026-09-26, run 14 — 15 d, NOT CLOSED, no new evidence.** Run 13's *neither kill* is carried, and it is named for what it is: **a discretionary override of the 14-day rung's literal binary, not an application of it** (`venture-critic` MINOR). The override is bounded: if Wed 2026-09-30's `texel-release` run ends without the screenshot path written here, run 15+ must pick one of the two kills. |
| T-010 | 2026-09-15 | **HUMAN** | **The paid product and the venture's whole internal record are both on a PUBLIC GitHub repo, and the state file says that repo is private.** `github.com/lizzxxaibot-alt/texel` returns `"private": false` from the API, was created on Texel's launch day, has its homepage field set to the itch page, and is pushed to **nightly by `pixelkiln-backup`** (last push 2026-09-15T03:09Z). It carries the full add-on source **and** `LEDGER.md`, `ACTIONS.md` (this file — which states in plain text that payouts are not set up), `SUPPORT.md`, `LISTING.md`, `OPERATIONS.md` (the full marketing and reply playbook), `PROGRESS.md`, `watch/`, `funnel/` (which records drop 1 as having shipped dead ink) and `promo/`. `raw.githubusercontent.com` serves them to an anonymous request; verified. **No credentials and no buyer emails are exposed** — `git ls-files` and a tracked-file grep both come back clean, and that was checked rather than assumed. **The measurement nobody had taken:** GitHub's traffic API reads **113 clones / 58 unique cloners in 14 days** against **5 repo page views (5 uniques, 4 of them referred from blenderartists.org)** and **1 visit referred onward to the itch page**. 58 cloners against 5 pageviews is the shape of automated mirroring rather than 58 people taking a $9.95 product for free, **but GitHub does not expose who cloned and this routine cannot close that gap** — what the number does settle is that the outbound flow is two orders of magnitude larger than the single inbound click, so "the repo refers more than it costs" is unsupported and must not be repeated. **This is ONE row on purpose.** Splitting it into "source = fine under GPL, docs = a question" was the first draft and `venture-critic` returned FATAL on it: `OPERATIONS.md` §7 rules on a **buyer** redistributing after purchase, not on the **seller** running a free zero-friction mirror that predates every sale, and pre-clearing the code half narrows what the user is actually being asked to weigh. **Recommendation, because judging well means handing over a specific one:** keep the source public per §7's spirit, and stop pushing `LEDGER.md`, `ACTIONS.md`, `SUPPORT.md`, `OPERATIONS.md`, `PROGRESS.md`, `watch/` and `promo/*.md` via `.gitignore` — then decide separately, with the clone data in hand, whether a self-published free mirror belongs under a paid listing that does not mention it. **0 stars, 0 forks, 0 issues: this is the cheapest this will ever be to change.** Owner is `HUMAN` because repo visibility is an account-level outward-facing setting and no routine may touch one. | the user records a decision on (a) repo visibility and (b) whether the internal operating files keep being pushed to it — either outcome closes this, including an explicit "leave it all public, deliberately", which is a valid answer and is what turns today's accident into a choice |
| T-011 | 2026-09-15 | **HUMAN** | **The Bluesky day-calendar is restated in three files and has now contradicted itself three times.** `texel-marketing`'s 09-14 run found its own `SKILL.md` §A table still reading `\| Sat \| #screenshotsaturday \|` while the prose two lines above it said Saturday moved to Sunday on 2026-09-09 and belongs to the packs — a run reading the table, which is the normal way to read a calendar, would have posted Texel on a Saturday using the packs' hashtag on the shared account. It fixed its own file and **handed the structural question here by name**: *"it is worth `texel-watch` asking whether the calendar should live in exactly one file rather than being restated in three."* This is the third instance of the defect class the 2026-09-09 routine audit was opened for. Owner is `HUMAN` because the fix means editing SKILL.md files belonging to three different routines, and no routine may edit another's. | `OPERATIONS.md` §3 is named as the single source for the day calendar and the SKILL.md copies are replaced by a pointer to it — **or** the user rules that restatement is fine and this is closed as won't-fix |
| T-015 | 2026-09-19 | **HUMAN** | **This routine's own verdict vocabulary has no rubric, it has now been raised twice without an owner, and it is the T-011 defect class in the watchguard's own output.** `watch/LOG.md` run 8 wrote it plainly and then did nothing with it: *"A note on the word DEGRADED, because it is now doing two different jobs … there is no written rubric for these labels and this routine should not mint one unilaterally."* Run 6 used **DEGRADED** for an unmeasured external exposure nobody had looked at (T-010); run 8 used it for a known internal failure another routine had already contained; **run 9 uses it again for a third mix** — a marquee-feature fix stranded off the store, a fleet-wide outage, and a `HUMAN` row four days from its 14-day line. One word, three severities, and a reader cannot tell them apart or see a trend across runs. `venture-critic` returned SERIOUS on carrying it a third time unowned (2026-09-19), and its reasoning is the one that matters: **this is exactly T-011's defect** — a thing restated repeatedly with no single owner — occurring in the routine's own vocabulary rather than in `ROADMAP.md` prose. Owner is `HUMAN` because the rubric belongs in `texel-watch`'s SKILL.md, **and a routine writing its own grading scale is how a gate stops biting** — the same reason §B's conversion threshold is being handed over rather than rewritten in place (see `watch/LOG.md` run 9 §B). **A deliberate "one word is fine, use your judgment" is a valid answer and closes this.** | `texel-watch`'s SKILL.md §E carries a written rubric saying what separates **healthy / degraded / blocked** — at minimum whether an unshipped-but-contained defect and an unmeasured external exposure are the same label — **or** the user rules that the single word is sufficient and this closes as won't-fix <br><br>**2026-09-25, run 13 — a fourth meaning, logged here rather than restated.** Run 13 uses DEGRADED with **zero live technical defects** on the listing: the grounds are commercial (0 sales in 16 days live, views decelerating) plus one doer routine missing slots. A fourth severity under one word. Still no rubric minted by this routine. <br><br>**2026-09-26, run 14 — a fifth use, on the same grounds as run 13.** DEGRADED again with zero live technical defects: 0 sales in 17 days live. `venture-critic` returned SERIOUS on a draft that did not log it here. Still no rubric minted by this routine. |
| T-016 | 2026-09-19 | **HUMAN** | **A BlenderNation submission is drafted, fact-checked and ready to send, and only a person can send it — the question is answerable today and nothing else depends on it.** `texel-funnel` mapped `blendernation.com/submit-news/` field by field on 2026-09-19 and drafted the post against the **live** 0.2.0 zip (`funnel/blendernation/SUBMISSION.md`); it deliberately omits every v0.2.1 feature, per `OPERATIONS.md` §3. **No account is required** — that was the open question and it is answered. Three independent blocks remain and any one is enough: **reCAPTCHA** is on the live page (read out of the DOM; `texel-funnel`'s SKILL.md §G is a hard stop), the form requires a **real name and email**, and BlenderNation publishes the article **under the user's name**. Entering personal data into a third party's form and publishing under one's own name are user decisions, not a routine's. **The argument for spending the effort is measured, not a hunch:** `blenderartists.org` has referred **8** of Texel's 76 views organically, four times what a whole free drop has produced, and BlenderNation is the same audience reached deliberately. **This row was split off from T-017 on `venture-critic`'s SERIOUS finding (2026-09-19):** the consent question does not depend on the missing hero image, and bundling them would have parked a same-day question behind a render until Fri 2026-09-25 for no reason. | a dated line in `funnel/blendernation/SUBMISSION.md` records the user's **yes or no**. A **no** is a complete answer and also kills T-017 |
| T-020 | 2026-09-26 | `texel-funnel` | **The one conversion mechanism still standing after the free-sibling-page retirement sits as a bullet, not a costed option.** `funnel/LOG.md` *Leads (added 2026-09-25)* names **Texel Lite / trial build** and says it needs the §0 dismissal test and `venture-critic` before any build. Nothing makes anyone come back to it, and 0 sales in 17 days is exactly when it matters. `venture-critic` (run 14) returned SERIOUS on a draft that left it as prose: CLAUDE.md §0 says a capability gap is a cost estimate, and a lead with no cost attached is how a dismissal happens by neglect. | `funnel/LOG.md` carries a dated entry for the lead with **(a)** what building it takes (effort, what gets cut out of the paid build, and whether itch permits a free demo file on a paid page or needs a separate project), **(b)** the prize in numbers from the Texel ledger, and **(c)** a GO / NO with `venture-critic`'s verdict attached — a NO must rest on the right-hand column of the §0 table |

## Closed

| id | opened | closed | owner | what happened |
|---|---|---|---|---|
| T-019 | 2026-09-21 | 2026-09-26 | `texel-funnel` | **CLOSED against the doer's recorded decision, and the reading it deferred to was taken independently.** `funnel/LOG.md` *T-019: the drop-2 window is declared VALID* was written 2026-09-25, before the 09-26 deadline, with `venture-critic` PROCEED. This run read `/game/summary/4991926` at ~17:05Z: 18 referrer rows (under the 20-row cap, so the table is complete), **no row for `texel-material-palettes`**, so drop 2 → Texel = **0**, under the floor of 3. **The pre-registered trigger fires: the free-sibling-itch-page format is retired.** The scope stays narrow: a Texel lite or trial build and Blender-community venues are **not** retired (lead now **T-020**). Also, per `venture-critic`: with n = 2 drops and 2 lifetime referrals, this stays a recommendation against Phase 2 drops, **not a settled finding about free content in general**. |
| T-017 | 2026-09-19 | 2026-09-26 | `texel-funnel` | **CLOSED on the done-when's second branch: the 5-round cap was reached without a pass, and it was recorded as CLAUDE.md §4 requires.** Verified in the files, not from the write-up: `funnel/blendernation/SUBMISSION.md` (13,542 B) carries the scoreboard rows for round 4 (flatness 0.241) and round 5 (0.210) against a pass line of 0.263, and the critic's findings, including the round 5 FATAL. `hero_1456x672.png` (r5, 1,129,013 B) and `hero_round4.png` (1,051,787 B) are both on disk. **No image is sendable.** The submission still waits on **T-016** (HUMAN), and a round 6 is not this row's to authorise. |
| T-012 | 2026-09-17 | 2026-09-25 | `texel-release` | **CLOSED, WITH A NAMED SUBSTITUTION ON PART (a) — judged as a substitution, not waved through.** (a) asked for the live zip to be downloaded and diffed against the stranded 0.2.1. **Not reachable by a read-only routine**: Texel is paid and itch answers `A key is needed to download` even to the owner; minting a key or buying our own product is a write this routine may not take; and the stranded artifact no longer exists. **Accepted in its place, each re-read by `texel-watch` run 13 on 2026-09-25 rather than taken from the doer's text:** cookie-less GET of the logged-out page → HTTP 200 / 31,466 B, widget `texel-0.2.1.zip` / `200 kB`; that size matches the local 205,427 B and separates from 0.2.0 (192,284 B, shown as 187 kB); the itch summary's file table lists 0.2.1 *uploaded a day ago*; and the hash-pinned uploaded copy (`dist/shipped/`, sha256 `e3cd2748…cbafaa2c`) contains both things the done-when names — `core/report.py`, and `symmetry_points` in `core/raster.py`. Residual risk: a different ~200 kB file uploaded under this name; four independent readings agreeing make that implausible. (b) devlog 1675244 live; `devlog.rss` now holds **3** items, newest Thu 24 Sep 2026 06:30 GMT (2 since 09-10). (c) `ROADMAP.md` *Post-mortem* section present; SKILL.md change recorded by the doer. **Original action:** **A release was reported as succeeded, ticked as SHIPPED, and never uploaded.** The 2026-09-16 run (18:00:16-18:17:54Z, status `succeeded`) did steps 1, 2, 4 and 6 of `ROADMAP.md`'s own *"How a release actually happens"* - built `dist/texel-0.2.1.zip` (197,505 B), gated it, bumped `blender_manifest.toml` and `bl_info` to 0.2.1, renumbered "Brush, the rest" to v0.2.2 - and **skipped step 3's upload and step 5's devlog, then ticked step 6 anyway**, writing *"SHIPPED 2026-09-16"* and *"v0.1.0, v0.2.0 and v0.2.1 are live"* into a file that **ships inside the customer zip**. Verified three independent ways on 09-17: logged-out page serves only `texel-0.2.0.zip, 187 kB`; dashboard **Uploads** holds only 0.1.0 and 0.2.0; analytics **File download counts** lists those two, *"uploaded 8 days ago"*. Devlog index still holds exactly 2 posts. **`texel-marketing` found this first**, on 09-16, wrote it up in `promo/QUEUE.md`, **refused to post the v0.2.1 beat over it**, and gated the Friday row on the upload - so the containment is already done and this row is only about the ship. **This is the THIRD time this file has asserted an untrue ship state** (T-003: the shipped zip claimed v0.1.0 *"has not shipped"*; T-006: the price anchored to a rival; now a version claimed live that is not), which is why the done-when asks for a gate and not just a file. The project already knows this move - `build.py` was taught on 09-16, three lines further down the same release, to fail when a module exists but is missing from its list. **The routine's own `succeeded` status is the thing that cannot be trusted here**: nothing between Wednesdays re-reads the live page, so without a read-back the same silent skip ships again on 09-23. **`texel-release` is WEEKLY (`0 10 * * 3`, next 2026-09-23)**, so this row reaches **6 days old** before its owner has a first opportunity - past the 3-day freeze and one day short of the 7-day toast, through no fault of the owner. That mismatch between a daily ladder and a weekly owner is noted here rather than silently absorbed. |
| T-013 | 2026-09-19 | 2026-09-25 | `texel-release` | **CLOSED on option (a).** `ROADMAP.md` v0.2.2 heading reads *target **2026-09-30** (was 26 Sep)* with the reason — `texel-release` fires `0 10 * * 3` and 26 Sep is a Saturday. Re-read by `texel-watch` run 13 at `ROADMAP.md:340`, not taken from the DONE? text. The doer's side note — a third cron-mismatch instance, the Wed 09-23 release slot not firing and catching up Thu 09-24T06:12Z — is the fleet scheduler's subject, `pixelkiln/launch/watch/ACTIONS.md` **A-037**, and is named in `watch/LOG.md` run 13 rather than re-filed here. **Original action:** **The v0.2.2 target falls on a day its owner never runs, and the one run before it is frozen by two of this ledger's own rows.** `ROADMAP.md` targets **v0.2.2 "Brush, the rest" for 2026-09-26**, and writes *"the honest reading is that v0.2.2 ships partial on 26 Sep"*. **2026-09-26 is a Saturday. `texel-release` fires `0 10 * * 3` — Wednesdays only** (confirmed off the task's own `cronExpression`, not off `OPERATIONS.md`, whose times are nominal). So the sentence describes a ship that no release run can execute: the runs either side are **Wed 2026-09-23** and **Wed 2026-09-30**. **And 09-23 is not free.** `texel-release`'s own SKILL.md §0 says *"A row assigned to you at 3+ days freezes you. Nothing else this run until it is cleared or refused."* On 09-23 it will own **T-008 at 12 days** and **T-012 at 6 days**, both past that line — so by its own rules it may not touch v0.2.2 until both are cleared or dated-refused. Neither the roadmap nor the release skill has noticed the collision, and the failure mode if nobody does is the one this ledger already has three instances of (T-003, T-006, T-012): a date arrives, nothing shipped, and the file gets ticked anyway. **This row is not asking for the feature and is not a judgment on scope** — the four v0.2.2 items are priced honestly in `ROADMAP.md` and `texel-release` owns that call. It asks only that the date stop describing something unreachable. **And the pattern, named so a third instance is not re-diagnosed from scratch (`venture-critic`, 2026-09-19):** this is the **second** time a weekly-cadence owner has been handed an externally-set deadline that does not line up with its own cron — T-012 exposed the daily escalation ladder against a weekly owner, and this row exposes a calendar target against a weekly owner. **A weekly routine can only ever act on its own weekday**, and any date set for it by another file has to be one of those weekdays or it is decoration. |
| T-002 | 2026-09-09 | 2026-09-23 | **HUMAN** | **RE-FILED STORE-WIDE, NOT DROPPED — `pixelkiln/launch/watch/ACTIONS.md` A-041.** Closed here on the user's instruction, 2026-09-23, after this run's 14-day report recommended re-filing rather than the ladder's kill/keep. **The ask is unchanged and unresolved**; only its address changed. **Why it moved:** this row lived in Texel's ledger, where Texel has earned **$0.00 across 83 views and 13.6 days live**, so the payout blocker cost this venture nothing and the row read, on age alone, like a dead finding worth killing — while every dollar it blocks is the packs', whose own ledger had no payout or tax row at all. A row filed against the venture it does not concern is the same untrackability **T-011** was opened for, one axis over. **Re-measured on the way out rather than handed over as inherited text**, and the inherited figures were stale: `/dashboard/payouts` read **2026-09-23T13:58:54Z** shows **Pending 1 / $4.26**, **Available 12 / $57.46** — **$61.72 across 13 payments, of which $57.46 is already past itch's 7-day hold**, against the **$51.20 / 11** `STATE.md` had been carrying since 2026-09-09 (corrected in place this run). Both blockers still live on the page: the tax interview is not done and neither PayPal nor Payoneer is connected. **Texel keeps a standing interest in A-041 and it is worth stating so a later run does not re-open this:** the moment Texel takes its first sale, its money lands in the same blocked balance. **This close does not mean the payout question is answered — it means it is now filed where the money is.** |
| T-018 | 2026-09-21 | 2026-09-23 | `texel-marketing` | **CLOSED on evidence re-verified independently of the doer's write-up.** The done-when asked for the Material Palettes beat in `promo/POSTED.md` with its asset path and a post URI verified on the public AppView. `texel-watch` run 12 did not take the doer's verification on trust and repeated it: a **cookie-less** `app.bsky.feed.getPostThread` against `at://pixelkiln.bsky.social/app.bsky.feed.post/3mw2cucqayg2o` returns the post live, author `pixelkiln.bsky.social` (`did:plc:qqgd26mw3b3zrlhuipf2vse6`), `createdAt` **2026-09-21T18:13:21.516Z**, `$type` `app.bsky.embed.images`, alt text present and intact. Every field matches what `texel-marketing` wrote in `promo/POSTED.md` under *Run 2026-09-21 (18:11-18:16Z)*, and `promo/QUEUE.md`'s Posted column carries the URI. **Late, not dropped, was the right call and the reason held up:** the beat landed with five days of `funnel/LOG.md`'s drop-2 window still to run, so **T-019**'s pre-registered experiment has a promotion arm rather than a second empty window. **What this close does NOT settle**, and run 12's ledger row says so in terms: the beat promotes a *sibling free product*, so the Texel channel itself is still dark — last beat about Texel **2026-09-16** — and the hypothesis test run 10 designed has still not started its real arm. |
| T-014 | 2026-09-19 | 2026-09-19 | `texel-funnel` | **Drop 2 shipped, and it was verified against the live web rather than against the doer's write-up.** `texel-funnel` ran 06:14:58 -> 06:46:39Z — its first run since 2026-09-11 — and published **Texel Material Palettes** (game 5025701, 24 eight-step `.gpl` ramps + master midtone sheet + reference PNG, CC0). `texel-watch` run 10 re-derived every clause of the done-when independently at 11:55Z: **HTTP 200 to a cookie-less `curl`** (26,689 B; `funnel/LOG.md` recorded 26,694 B at ~06:45Z — **a 5-byte difference, not a byte-identical match, and it is the page's own view/download counters moving between the two reads**, not a discrepancy to wave at); **payment mode renders publicly as "Name your own price"**, which is the file's pre-registered held-constant variable; **exactly 1 `<a href>` to `z3er1n.itch.io/texel`** in the description, counted on the fetched page — drop 1's exact failure, caught before publishing this time; **AI disclosure renders as `AI Assisted, Code, Graphics, Text`**; and itch Project totals now carries its own row, **`Texel Material Palettes \| 9 views \| 2 downloads \| 2 collections`** (1 of the 2 downloads is the funnel's own logged verification grab, so **1 stranger grab in the first ~5 h**). The success bar is pre-registered in `funnel/LOG.md` **before** the window opened — **5+ referred visits to Texel by 2026-09-26**, secondary 40+ views, and **retirement of the whole free-drop format if referrals come in under 3**. **BlenderNation was explicitly not a condition on this row and is written up in full as `HUMAN`-blocked; it does not vanish with this closure — it is reopened as T-016 (the consent, `HUMAN`) and T-017 (the required hero image, `texel-funnel`).** |
| T-003 | 2026-09-09 | 2026-09-09 | user | `dist/texel-0.1.0.zip` shipped a `ROADMAP.md` reading *"v0.1.0 … has not shipped"*. Found by `texel-support`, fixed by `texel-release` shipping v0.2.0 at 19:25 — the live page now serves only 0.2.0. |
| T-004 | 2026-09-09 | 2026-09-09 | user | The download-page instructions named `texel-0.1.0.zip` three hours after 0.2.0 became the live file. Made version-agnostic (`texel-*.zip`). |
| T-005 | 2026-09-09 | 2026-09-09 | user | **The operator count contradicted itself on a live sales page** — description said 95, cover and banner said 94. Real count is 95 (`TEXEL_OP_COUNT=95`, register() in Blender 4.5.9). Both visuals re-rendered and republished. |
| T-006 | 2026-09-09 | 2026-09-09 | user | `ROADMAP.md` — **which ships inside the customer zip** — anchored the price to another add-on's $29.90, breaching the standing no-rivalry rule that had been applied to `LISTING.md` the same day. Removed. |
| T-001 | 2026-09-09 | 2026-09-11 | `texel-marketing` | **CLOSED against evidence, verified independently this run.** The density readout still is **live as gallery slot 10** on `z3er1n.itch.io/texel` — the logged-out page (HTTP 200, 30,963 B) renders **10 `screenshot_list` images**, up from 9, and the asset id `EcU1Ox` the doer recorded is present in that list. `LISTING.md` line 386 is struck. The doer's build evidence stands on its own terms: `density_shot.py` drives a real Blender 4.5.9, nothing on the card is typed, `make_density.py` asserts the 8.4x = 3.2/0.38 claim so the card fails to build if it stops holding, and the CLAUDE.md §4 loop ran to **round 3 VERDICT: SHIP, zero FATAL, zero SERIOUS**. The product defect the doer surfaced while building it is **not** closed with this row — it is now **T-008**, owned by `texel-release`. |
| T-007 | 2026-09-10 | 2026-09-11 | `texel-marketing` | **CLOSED against evidence, verified this run.** `promo/QUEUE.md` now carries a **Sun 09-13** row (beat: reserved for `texel-funnel`'s first drop, fallback the density card with its asset path) **and** a named section, *"Sunday 09-13 exists, and it is the funnel's"*, which says in words what a blank row cannot: the slot is unclaimed-and-reserved rather than non-existent. The over-ceiling cost is stated rather than hidden, and **Mon 09-14 is named as the beat that gives way**, not Sunday. `texel-funnel`'s first ever run is today at 10:07 and its gate — *"`texel-marketing` has a free slot in the next two days"* — is now readable as satisfied from inside `QUEUE.md` alone. Whether the funnel actually ships is its own run's business and will be judged tomorrow, not here. |
| T-009 | 2026-09-14 | 2026-09-15 | `texel-funnel` | **CLOSED against evidence the doer wrote — a filled row in its own file, not a promise.** `funnel/LOG.md` now carries drop 1's result row filled from exactly the two `LEDGER.md` readings the row required — **46 → 63 = +17** over the clean 72 h interval — and judges it in words: *"Window closed 2026-09-14. Read it straight: drop 1 missed its own bar."* It answers the theatre half too (8 stranger file-grabs off 23 views, stated as 8 rather than the flattering 11, because three were `dl_check.mjs`), and it **corrects its own pre-registered bar downward** — "~30" was one day's +10 extrapolated as a rate, the honest bar was ~20, and the drop misses either way. **The timing explains why run 5 could not see it, and it is a method correction future runs need.** `list_scheduled_tasks` gives `texel-funnel` `lastRunAt` **2026-09-11T15:07Z**, which is a **start** time; `list_task_runs` gives that same session `last_activity_at` **2026-09-14T13:34Z**. The run was still alive three days later and wrote this row at 13:33Z on 09-14, **100 minutes after run 5 opened T-009 at 11:53Z**. So the row was correct when written and was cleared the same day. **Read `last_activity_at`, not `lastRunAt`, before judging whether a routine has finished.** The drop-2 clean-test commitment and the BlenderNation lead are the funnel's own agenda for Friday, not conditions on this row. |
